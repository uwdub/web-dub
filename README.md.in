# invoke-base

{% if readme.travis_badge %}
{{ readme.travis_badge }}
{% endif %}

A fogies project used as a template for other projects.

Provides for dependency management, tasks via the invoke tools, and testing.

{% include "base/readme/install_dependencies.md" %}
